name: MacOS

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  BUILD_TYPE: Debug
  BUILD_TYPE_LOWER: debug

jobs:
  build:
    runs-on: macos-12

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
    
    - name: Get Conan
      uses: turtlebrowser/get-conan@v1.0
      
    - name: Create default profile
      run: conan profile detect

    - name: install dependencies
      run: conan install . --build=missing --settings=build_type=${{env.BUILD_TYPE}} -s compiler.cppstd=17

    - name: Configure CMake
      run: cmake -S . --preset conan-${{env.BUILD_TYPE_LOWER}} -DENABLE_TEST_COVERAGE=1

    - name: Build
      run: cmake --build --preset conan-${{env.BUILD_TYPE_LOWER}}

    - name: Test
      run: ctest --preset conan-${{env.BUILD_TYPE_LOWER}}
