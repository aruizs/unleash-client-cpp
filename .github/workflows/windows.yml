name: Windows

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  BUILD_TYPE: Release
  BUILD_TYPE_LOWER: release

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
    
    - name: Get Conan
      uses: turtlebrowser/get-conan@v1.0
      
    - name: Create default profile
      run: conan profile detect

    - name: install dependencies
      run: conan install . --build=missing --settings=build_type=${{env.BUILD_TYPE}} -s compiler.cppstd=17

    - name: Configure CMake
      run: cmake -S . --preset conan-default

    - name: Build
      run: cmake --build --preset conan-${{env.BUILD_TYPE_LOWER}} -j10

    - name: Test
      run: ctest --preset conan-${{env.BUILD_TYPE_LOWER}}
